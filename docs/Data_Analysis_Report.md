# Data Analysis - Project 5

5,000 instances; ~28% fail. Failures correlated with risky tests + change keywords (refund, jwt, lock, migration, etc.). Stable tests + doc-only commits predominantly pass. Historical fail rate included as weak signal in text.
