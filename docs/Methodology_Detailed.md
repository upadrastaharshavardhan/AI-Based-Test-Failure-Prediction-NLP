# Methodology - Project 5

Textual representation of test + change context embedded with MiniLM. Logistic Regression (balanced) predicts pass/fail. FAISS retrieves similar historical tests. Training uses stratified split; evaluation reports accuracy, macro F1, fail-class F1, ROC-AUC.
